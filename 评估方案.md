# 智能开发助手核心辅助代码开发功能评估方案

## 一、评估目标

对 Agent 在**核心辅助代码开发功能**上的实际能力进行端到端评估，包括：

- **Bug 修复**：定位缺陷并修复，使失败的测试通过
- **功能实现**：根据需求编写新代码，通过预设测试
- **代码重构**：在不改变外部行为的前提下改善代码结构
- **代码审查**：识别代码问题并给出有效修复建议
- **调试定位**：根据错误堆栈和日志定位根因

评估的核心原则：**以 Agent 产出的代码在真实执行后的结果为准**，而非仅依据 Agent 的文本回复判断成败。

## 二、评估架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      评估框架架构                                 │
│                                                                   │
│  ┌───────────┐    ┌───────────┐    ┌───────────┐    ┌────────┐ │
│  │  Suite    │ →  │   Task    │ →  │  Runner   │ →  │ Grader │ │
│  │  (题库)   │    │ (单测试)  │    │ (执行器)  │    │(评分器)│ │
│  └───────────┘    └───────────┘    └───────────┘    └────────┘ │
│       │                │                │                │      │
│       │                │                │                │      │
│   多个 task       含 prompt、       多次运行、       多维度       │
│   按主题分组       初始代码、       记录完整         评分：        │
│                   预期结果          日志             正确性、     │
│                                                    效率、        │
│                                                    鲁棒性、      │
│                                                    工具使用、    │
│                                                    上下文管理    │
└─────────────────────────────────────────────────────────────────┘
```

### 三层结构说明

| 层级 | 名称 | 职责 | 示例 |
|---|---|---|---|
| **Task** | 单测试用例 | 最小评估单元，包含一个具体的代码开发任务 | "修复 `parse_user_input` 不校验 `=` 的 bug" |
| **Suite** | 题库 | 多个相关 Task 的集合，按主题或难度分组 | "Bug 修复题库"、"功能实现题库" |
| **Grader** | 评分器 | 对 Task 执行结果进行多维度评分 | 正确性 80%、效率 70%、工具使用 85% |

## 三、Task 设计规范

### 3.1 Task 数据结构

每个 Task 是一个 JSON 文件，包含以下字段：

```json
{
  "task_id": "bugfix-parser-001",
  "suite_id": "bug_fix",
  "title": "修复 parse_user_input 未校验等号的 bug",
  "difficulty": "easy",
  "prompt": "tests/test_parser.py::TestParseUserInput::test_parse_invalid_input_raises 失败了，报错显示 parse_user_input('=no_key') 没有抛 ValueError 而是返回了 {'': 'no_key'}，请修复这个 bug。",
  "initial_files": {
    "src/parser.py": "<初始含 bug 的源码>",
    "tests/test_parser.py": "<测试文件>"
  },
  "evaluation": {
    "type": "pytest",
    "command": "python -m pytest tests/test_parser.py -v",
    "expected_pass": ["test_parse_invalid_input_raises", "test_parse_simple_key_value"],
    "expected_fail": [],
    "exit_code": 0
  },
  "graders": [
    {"dimension": "correctness", "type": "test_pass_rate"},
    {"dimension": "no_regression", "type": "all_tests_pass"},
    {"dimension": "code_quality", "type": "lint", "command": "flake8 src/parser.py"},
    {"dimension": "minimal_change", "type": "diff_size", "max_lines": 10}
  ],
  "time_limit_seconds": 300,
  "max_tool_calls": 30
}
```

### 3.2 Task 分类与题库设计

#### Suite 1：Bug 修复（bug_fix）

| Task ID | 难度 | 场景描述 | 评估重点 |
|---|---|---|---|
| bugfix-parser-001 | easy | parse_user_input 未校验 `=` | 测试通过 + 无回归 |
| bugfix-offbyone-002 | medium | off-by-one 错误在循环边界 | 定位准确 + 修复最小 |
| bugfix-race-003 | hard | Redis 并发竞态导致超卖 | 根因定位 + 原子操作修复 |
| bugfix-leak-004 | medium | 文件句柄未关闭导致内存泄漏 | 资源管理 + 上下文管理器 |
| bugfix-encoding-005 | medium | 中文编码导致的 UnicodeDecodeError | 编码处理 + 边界覆盖 |
| bugfix-null-006 | medium | 空值未校验导致 NullPointerException | 防御性编程 |
| bugfix-tz-007 | hard | 时区处理错误导致跨时区计算错误 | 时区逻辑 + 测试覆盖 |

#### Suite 2：功能实现（feature_impl）

| Task ID | 难度 | 场景描述 | 评估重点 |
|---|---|---|---|
| feat-crud-001 | easy | 实现用户 CRUD 接口 | 功能完整 + 测试通过 |
| feat-pagination-002 | medium | 实现分页查询 | 边界处理 + 性能 |
| feat-cache-003 | medium | 添加 Redis 缓存层 | 缓存一致性 + 失效策略 |
| feat-export-004 | medium | 实现 CSV 导出功能 | 流式处理 + 大文件支持 |
| feat-websocket-005 | hard | 实现 WebSocket 实时推送 | 并发处理 + 连接管理 |
| feat-auth-006 | hard | 实现 JWT 认证中间件 | 安全性 + Token 刷新 |

#### Suite 3：代码重构（refactor）

| Task ID | 难度 | 场景描述 | 评估重点 |
|---|---|---|---|
| refactor-godclass-001 | hard | 拆分上帝类为职责单一的类 | 行为保持 + 结构改善 |
| refactor-duplicate-002 | medium | 提取重复代码为公共方法 | DRY 原则 + 无回归 |
| refactor-long-method-003 | medium | 拆分 100 行长方法 | 可读性 + 行为保持 |
| refactor-nested-if-004 | easy | 扁平化深层嵌套条件 | 卫语句 + 可读性 |

#### Suite 4：代码审查（code_review）

| Task ID | 难度 | 场景描述 | 评估重点 |
|---|---|---|---|
| review-security-001 | hard | 审查含 SQL 注入的代码 | 安全漏洞识别 |
| review-perf-002 | medium | 审查 N+1 查询问题 | 性能问题识别 |
| review-style-003 | easy | 审查命名和结构问题 | 代码规范 |

#### Suite 5：调试定位（debugging）

| Task ID | 难度 | 场景描述 | 评估重点 |
|---|---|---|---|
| debug-stacktrace-001 | medium | 根据堆栈跟踪定位 bug | 堆栈分析 + 根因定位 |
| debug-log-002 | hard | 根据应用日志定位生产问题 | 日志分析 + 根因定位 |
| debug-concurrent-003 | hard | 定位并发死锁问题 | 并发分析 + 死锁解除 |

### 3.3 初始代码与预期结果的隔离

为保证评估公平性，每个 Task 的执行环境必须完全隔离：

```
eval_workspace/
├── task_<task_id>_<run_id>/
│   ├── src/              # 初始源码（从 task.initial_files 复制）
│   ├── tests/            # 测试文件
│   ├── .agent_logs/      # Agent 思考和工具调用日志
│   │   ├── messages.jsonl     # 完整对话历史
│   │   ├── tool_calls.jsonl   # 工具调用记录
│   │   └── tool_results.jsonl # 工具执行结果
│   ├── .agent_output/    # Agent 修改后的文件
│   └── .grader_results/  # 评分结果
```

## 四、对抗模型随机性的多次尝试机制

### 4.1 多轮运行策略

```python
# 评估配置
EVAL_CONFIG = {
    "runs_per_task": 5,          # 每个 task 运行 5 次
    "temperature": 0.7,          # 使用默认温度，不人为降低
    "timeout_per_run": 300,      # 单次运行超时 5 分钟
    "max_tool_calls": 30,        # 单次运行最多 30 次工具调用
    "isolate_workspace": True,   # 每次运行独立工作目录
}
```

### 4.2 统计方法

对每个 Task 的 5 次运行结果，统计以下指标：

| 统计指标 | 计算方式 | 含义 |
|---|---|---|
| **通过率（pass_rate）** | 通过次数 / 总运行次数 | 核心能力指标，如 5 次中 4 次通过 = 80% |
| **平均工具调用数** | 所有运行的工具调用数均值 | 效率指标 |
| **平均耗时** | 所有运行的墙钟时间均值 | 效率指标 |
| **首次通过轮次** | 首次通过时的运行序号 | 稳定性指标 |
| **结果方差** | 各维度评分的标准差 | 稳定性指标 |

### 4.3 通过率判定标准

| 通过率 | 评级 | 含义 |
|---|---|---|
| 100%（5/5） | A | 稳定通过 |
| 80%（4/5） | B | 基本通过，偶发失败 |
| 60%（3/5） | C | 不稳定，能力边界 |
| ≤ 40%（≤2/5） | D | 不通过，存在缺陷 |

## 五、完整日志记录机制

### 5.1 日志记录的内容

每次 Task 运行需记录以下 3 类日志：

#### 日志 1：messages.jsonl（完整对话历史）

```jsonl
{"turn": 1, "role": "user", "content": "修复 parse_user_input 的 bug...", "timestamp": "2026-08-05T12:00:00"}
{"turn": 2, "role": "assistant", "content": [{"type": "text", "text": "我来看看测试文件..."}, {"type": "tool_use", "id": "tool-1", "name": "read_file", "input": {"path": "tests/test_parser.py"}}], "model": "claude-sonnet-4-6", "stop_reason": "tool_use", "input_tokens": 1234, "output_tokens": 567, "timestamp": "2026-08-05T12:00:02"}
{"turn": 3, "role": "user", "content": [{"type": "tool_result", "tool_use_id": "tool-1", "content": "...测试文件内容..."}], "timestamp": "2026-08-05T12:00:02"}
```

#### 日志 2：tool_calls.jsonl（工具调用记录）

```jsonl
{"call_id": "tool-1", "turn": 2, "tool": "read_file", "input": {"path": "tests/test_parser.py"}, "output_length": 1234, "duration_ms": 5, "success": true, "timestamp": "2026-08-05T12:00:02"}
{"call_id": "tool-2", "turn": 4, "tool": "bash", "input": {"command": "python -m pytest tests/test_parser.py -v"}, "output_length": 5678, "duration_ms": 2300, "success": true, "timestamp": "2026-08-05T12:00:05"}
{"call_id": "tool-3", "turn": 6, "tool": "edit_file", "input": {"path": "src/parser.py", "old_text": "...", "new_text": "..."}, "output_length": 20, "duration_ms": 3, "success": true, "timestamp": "2026-08-05T12:00:08"}
```

#### 日志 3：tool_results.jsonl（工具执行结果）

```jsonl
{"call_id": "tool-1", "tool": "read_file", "result": "...完整文件内容...", "truncated": false}
{"call_id": "tool-2", "tool": "bash", "result": "...pytest 输出...", "exit_code": 1, "stdout": "...", "stderr": "..."}
```

### 5.2 日志记录的实现方式

通过包装 Agent 的 `agent_loop` 和 `TOOL_HANDLERS`，在不修改原代码的前提下注入日志记录：

```python
class LoggedAgentLoop:
    """带完整日志记录的 Agent Loop 包装器。"""

    def __init__(self, log_dir: Path):
        self.log_dir = log_dir
        self.log_dir.mkdir(parents=True, exist_ok=True)
        self.messages_log = open(log_dir / "messages.jsonl", "w", encoding="utf-8")
        self.tool_calls_log = open(log_dir / "tool_calls.jsonl", "w", encoding="utf-8")
        self.tool_results_log = open(log_dir / "tool_results.jsonl", "w", encoding="utf-8")
        self.turn = 0

    def log_user_message(self, content):
        self.turn += 1
        self._write_jsonl(self.messages_log, {
            "turn": self.turn, "role": "user",
            "content": content, "timestamp": datetime.now().isoformat()
        })

    def log_assistant_response(self, response):
        self.turn += 1
        self._write_jsonl(self.messages_log, {
            "turn": self.turn, "role": "assistant",
            "content": self._serialize_content(response.content),
            "model": response.model,
            "stop_reason": response.stop_reason,
            "input_tokens": response.usage.input_tokens,
            "output_tokens": response.usage.output_tokens,
            "timestamp": datetime.now().isoformat()
        })

    def log_tool_call(self, block, output, duration_ms, success):
        self._write_jsonl(self.tool_calls_log, {
            "call_id": block.id, "turn": self.turn, "tool": block.name,
            "input": block.input, "output_length": len(str(output)),
            "duration_ms": duration_ms, "success": success,
            "timestamp": datetime.now().isoformat()
        })
        self._write_jsonl(self.tool_results_log, {
            "call_id": block.id, "tool": block.name,
            "result": str(output)[:50000], "truncated": len(str(output)) > 50000
        })

    def _write_jsonl(self, f, data):
        f.write(json.dumps(data, ensure_ascii=False, default=str) + "\n")
        f.flush()

    def close(self):
        self.messages_log.close()
        self.tool_calls_log.close()
        self.tool_results_log.close()
```

### 5.3 工具调用计时包装

```python
import time
from functools import wraps

def timed_tool_handler(handler):
    """包装工具处理器，记录耗时和成功状态。"""
    @wraps(handler)
    def wrapper(**kwargs):
        t0 = time.perf_counter()
        try:
            result = handler(**kwargs)
            success = not str(result).startswith("Error:")
            return result, time.perf_counter() - t0, success
        except Exception as e:
            return f"Error: {e}", time.perf_counter() - t0, False
    return wrapper
```

## 六、基于实际代码结果的评估机制

### 6.1 核心原则：执行验证而非文本判断

评估 Agent 产出的代码时，**不依赖 LLM 判断代码是否正确**，而是通过实际执行验证：

| 评估方式 | 说明 | 示例 |
|---|---|---|
| **测试用例执行** | 运行预设的 pytest 测试，检查通过情况 | `python -m pytest tests/ -v` |
| **代码执行** | 直接运行 Agent 产出的代码，检查输出 | `python src/main.py --input test.txt` |
| **Lint 检查** | 运行 flake8/pylint 检查代码质量 | `flake8 src/ --max-complexity=10` |
| **类型检查** | 运行 mypy 检查类型正确性 | `mypy src/ --strict` |
| **Diff 分析** | 分析 Agent 修改的代码量是否最小化 | `git diff --stat` |
| **文件存在性** | 检查 Agent 是否创建了预期的文件 | `assert Path("src/new_module.py").exists()` |

### 6.2 评估执行流程

```python
class TaskRunner:
    """Task 执行器：在隔离环境中运行 Agent，收集产出。"""

    def run_task(self, task: dict, run_id: int) -> dict:
        """运行单个 task 一次，返回执行结果。"""
        # 1. 创建隔离工作目录
        workspace = self._create_workspace(task, run_id)

        # 2. 写入初始文件
        self._write_initial_files(workspace, task["initial_files"])

        # 3. 启动带日志记录的 Agent
        logger = LoggedAgentLoop(workspace / ".agent_logs")
        agent_result = self._run_agent(workspace, task["prompt"], logger, task)

        # 4. 执行评估命令（在 Agent 修改后的代码上）
        eval_result = self._run_evaluation(workspace, task["evaluation"])

        # 5. 运行评分器
        grader_results = self._run_graders(workspace, task["graders"])

        # 6. 收集完整结果
        return {
            "task_id": task["task_id"],
            "run_id": run_id,
            "agent_finished": agent_result["finished"],
            "agent_duration_s": agent_result["duration"],
            "tool_call_count": agent_result["tool_calls"],
            "evaluation": eval_result,
            "graders": grader_results,
            "logs": str(workspace / ".agent_logs"),
        }

    def _run_evaluation(self, workspace: Path, eval_config: dict) -> dict:
        """执行评估命令，返回测试结果。"""
        import subprocess
        result = subprocess.run(
            eval_config["command"], shell=True, cwd=workspace,
            capture_output=True, text=True, timeout=60
        )
        # 解析 pytest 输出，提取通过/失败的测试
        passed, failed = self._parse_pytest_output(result.stdout)
        return {
            "exit_code": result.returncode,
            "expected_exit_code": eval_config["exit_code"],
            "passed_tests": passed,
            "failed_tests": failed,
            "expected_pass": eval_config["expected_pass"],
            "expected_fail": eval_config["expected_fail"],
            "stdout": result.stdout[:10000],
            "stderr": result.stderr[:5000],
        }
```

## 七、多维度评分器（Grader）

### 7.1 评分维度定义

| 维度 | 权重 | 满分标准 | 评分方式 |
|---|---|---|---|
| **正确性（correctness）** | 30% | 所有预期测试通过 | 测试通过率 × 100 |
| **无回归（no_regression）** | 20% | 原有测试全部通过 | 原有测试通过率 × 100 |
| **代码质量（code_quality）** | 15% | Lint 无错误 | 100 - (error数 × 10 + warning数 × 2) |
| **效率（efficiency）** | 15% | 工具调用少、耗时短 | 基于工具调用数和耗时的归一化评分 |
| **工具使用合理性（tool_usage）** | 10% | 工具选择恰当、无冗余调用 | 基于工具调用日志的启发式分析 |
| **上下文管理（context_mgmt）** | 10% | 无不必要的压缩、上下文利用合理 | 基于对话长度和压缩触发分析 |

### 7.2 各维度评分器实现

#### 评分器 1：正确性（correctness）

```python
def grade_correctness(eval_result: dict) -> dict:
    """评分正确性：预期通过的测试是否全部通过。"""
    expected_pass = set(eval_result["expected_pass"])
    actual_pass = set(eval_result["passed_tests"])
    # 预期通过的测试中实际通过的比例
    pass_rate = len(expected_pass & actual_pass) / len(expected_pass) if expected_pass else 0
    return {
        "dimension": "correctness",
        "score": round(pass_rate * 100, 1),
        "detail": {
            "expected": list(expected_pass),
            "actual": list(actual_pass),
            "missing": list(expected_pass - actual_pass),
        }
    }
```

#### 评分器 2：无回归（no_regression）

```python
def grade_no_regression(eval_result: dict, initial_tests: list) -> dict:
    """评分无回归：原有通过的测试是否仍然通过。"""
    initial_pass = set(initial_tests)
    actual_pass = set(eval_result["passed_tests"])
    regression = initial_pass - actual_pass
    score = (len(initial_pass) - len(regression)) / len(initial_pass) * 100 if initial_pass else 100
    return {
        "dimension": "no_regression",
        "score": round(score, 1),
        "detail": {"regressed_tests": list(regression)}
    }
```

#### 评分器 3：代码质量（code_quality）

```python
def grade_code_quality(workspace: Path) -> dict:
    """评分代码质量：通过 flake8 检查。"""
    import subprocess
    result = subprocess.run(
        "flake8 src/ --max-complexity=10 --max-line-length=120",
        shell=True, cwd=workspace, capture_output=True, text=True
    )
    errors = [l for l in result.stdout.splitlines() if "E" in l.split(":")[1][:2]]
    warnings = [l for l in result.stdout.splitlines() if "W" in l.split(":")[1][:2]]
    score = max(0, 100 - len(errors) * 10 - len(warnings) * 2)
    return {
        "dimension": "code_quality",
        "score": score,
        "detail": {"errors": len(errors), "warnings": len(warnings), "output": result.stdout[:2000]}
    }
```

#### 评分器 4：效率（efficiency）

```python
def grade_efficiency(run_result: dict, baseline: dict = None) -> dict:
    """评分效率：基于工具调用数和耗时。"""
    tool_calls = run_result["tool_call_count"]
    duration = run_result["agent_duration_s"]
    # 基准：期望工具调用数 10，期望耗时 60s
    expected_calls = baseline.get("tool_calls", 10) if baseline else 10
    expected_duration = baseline.get("duration", 60) if baseline else 60
    call_score = min(100, expected_calls / max(tool_calls, 1) * 100)
    time_score = min(100, expected_duration / max(duration, 1) * 100)
    score = (call_score + time_score) / 2
    return {
        "dimension": "efficiency",
        "score": round(score, 1),
        "detail": {"tool_calls": tool_calls, "duration_s": duration,
                   "expected_calls": expected_calls, "expected_duration": expected_duration}
    }
```

#### 评分器 5：工具使用合理性（tool_usage）

```python
def grade_tool_usage(tool_calls_log: list) -> dict:
    """评分工具使用合理性：检查是否有冗余调用、工具选择是否恰当。"""
    issues = []
    # 检查 1：连续多次读取同一文件
    read_paths = [c["input"]["path"] for c in tool_calls_log if c["tool"] == "read_file"]
    duplicates = [p for p in read_paths if read_paths.count(p) > 2]
    if duplicates:
        issues.append(f"重复读取文件: {set(duplicates)}")
    # 检查 2：用 bash 的 cat/grep 而非专用工具
    bash_cmds = [c["input"]["command"] for c in tool_calls_log if c["tool"] == "bash"]
    for cmd in bash_cmds:
        if cmd.strip().startswith("cat "):
            issues.append(f"用 bash cat 而非 read_file: {cmd[:50]}")
        if cmd.strip().startswith("grep ") and not cmd.strip().startswith("grep -r"):
            issues.append(f"用 bash grep 而非专用搜索: {cmd[:50]}")
    # 检查 3：edit_file 失败后未读取文件就重试
    for i, c in enumerate(tool_calls_log):
        if c["tool"] == "edit_file" and not c["success"]:
            if i + 1 < len(tool_calls_log):
                next_call = tool_calls_log[i + 1]
                if next_call["tool"] == "edit_file" and next_call["input"]["path"] == c["input"]["path"]:
                    issues.append(f"edit_file 失败后未读取就重试: {c['input']['path']}")
    score = max(0, 100 - len(issues) * 15)
    return {"dimension": "tool_usage", "score": score, "detail": {"issues": issues}}
```

#### 评分器 6：上下文管理（context_mgmt）

```python
def grade_context_mgmt(messages_log: list, tool_calls_log: list) -> dict:
    """评分上下文管理：检查压缩触发是否合理、上下文是否有效利用。"""
    issues = []
    # 检查 1：过早触发压缩（对话还很短就压缩）
    total_messages = len(messages_log)
    compress_calls = [c for c in tool_calls_log if c["tool"] == "compress"]
    if total_messages < 20 and compress_calls:
        issues.append(f"对话仅 {total_messages} 条就触发压缩")
    # 检查 2：忽略工具结果（工具返回后未利用就继续提问）
    tool_results_turns = [m["turn"] for m in messages_log if m["role"] == "user"
                          and isinstance(m.get("content"), list)
                          and any(isinstance(b, dict) and b.get("type") == "tool_result"
                                  for b in m["content"])]
    # 检查 3：重复提问已通过工具获取的信息
    # 简化检查：统计 assistant 消息中是否包含 "what is" 或 "show me" 等冗余请求
    for m in messages_log:
        if m["role"] == "assistant":
            content = str(m.get("content", ""))
            if "let me read" in content.lower() and tool_results_turns:
                pass  # 正常行为
    score = max(0, 100 - len(issues) * 20)
    return {"dimension": "context_mgmt", "score": score, "detail": {"issues": issues}}
```

### 7.3 综合评分计算

```python
def compute_overall_score(grader_results: list) -> dict:
    """计算综合评分，返回加权总分和各维度得分。"""
    weights = {
        "correctness": 0.30,
        "no_regression": 0.20,
        "code_quality": 0.15,
        "efficiency": 0.15,
        "tool_usage": 0.10,
        "context_mgmt": 0.10,
    }
    dimension_scores = {g["dimension"]: g["score"] for g in grader_results}
    overall = sum(dimension_scores.get(k, 0) * w for k, w in weights.items())
    return {
        "overall_score": round(overall, 1),
        "dimension_scores": dimension_scores,
        "grade": _score_to_grade(overall),
    }

def _score_to_grade(score: float) -> str:
    if score >= 90: return "A"
    if score >= 80: return "B"
    if score >= 70: return "C"
    if score >= 60: return "D"
    return "F"
```

## 八、通过评估发现 Agent 缺陷的方法

### 8.1 缺陷发现流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  运行评估     │ →  │  聚合结果     │ →  │  定位缺陷     │ →  │  归因分析     │
│  (5次/task)  │    │  (统计)      │    │  (低分维度)  │    │  (日志分析)  │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
```

### 8.2 缺陷定位矩阵

根据各维度评分组合，定位 Agent 的具体缺陷类型：

| 现象 | 正确性 | 无回归 | 代码质量 | 效率 | 工具使用 | 可能的缺陷 |
|---|---|---|---|---|---|---|
| 测试不通过 | 低 | - | - | - | - | 代码逻辑错误或理解需求偏差 |
| 原有测试失败 | 高 | 低 | - | - | - | 修改引入回归，缺乏全局视角 |
| 代码风格差 | 高 | 高 | 低 | - | - | 缺乏代码规范意识 |
| 工具调用过多 | 高 | 高 | - | 低 | 低 | 冗余操作、缺乏规划 |
| 工具选择不当 | - | - | - | - | 低 | 对工具系统能力理解不足 |
| 过早压缩 | - | - | - | - | - | 上下文管理策略过于激进 |
| 随机性大 | 方差大 | - | - | - | - | 决策不稳定，缺乏确定性策略 |

### 8.3 日志驱动的归因分析

当某个 Task 评分较低时，通过日志分析定位根因：

#### 分析步骤 1：查看工具调用序列

```python
def analyze_failure(log_dir: Path) -> dict:
    """分析失败 Task 的日志，定位根因。"""
    tool_calls = [json.loads(l) for l in (log_dir / "tool_calls.jsonl").read_text().splitlines()]

    # 1. 工具调用时序分析
    sequence = [(c["turn"], c["tool"], c["input"], c["success"]) for c in tool_calls]

    # 2. 识别异常模式
    patterns = {
        "反复读取同一文件": _check_repeated_reads(tool_calls),
        "edit_file 连续失败": _check_edit_failures(tool_calls),
        "未运行测试就提交": _check_no_test_before_complete(tool_calls),
        "bash 命令超时": _check_timeouts(tool_calls),
        "工具调用后无后续行动": _check_idle_after_result(tool_calls),
    }
    return {"sequence": sequence, "patterns": {k: v for k, v in patterns.items() if v}}
```

#### 分析步骤 2：检查对话上下文

```python
def analyze_context(log_dir: Path) -> dict:
    """分析对话上下文，检查信息利用情况。"""
    messages = [json.loads(l) for l in (log_dir / "messages.jsonl").read_text().splitlines()]

    return {
        "total_turns": len(messages),
        "total_tokens": sum(m.get("input_tokens", 0) + m.get("output_tokens", 0) for m in messages),
        "compression_triggered": any("compress" in str(m.get("content", "")) for m in messages),
        "tool_results_ignored": _find_ignored_tool_results(messages),
        "redundant_questions": _find_redundant_questions(messages),
    }
```

#### 分析步骤 3：对比成功与失败运行

```python
def compare_runs(success_log: Path, failure_log: Path) -> dict:
    """对比同一 task 的成功和失败运行，找出差异。"""
    success_calls = _load_tool_calls(success_log)
    failure_calls = _load_tool_calls(failure_log)

    return {
        "success_tool_count": len(success_calls),
        "failure_tool_count": len(failure_calls),
        "first_divergence": _find_first_divergence(success_calls, failure_calls),
        "success_has_test_run": any(c["tool"] == "bash" and "pytest" in c["input"].get("command", "")
                                    for c in success_calls),
        "failure_has_test_run": any(c["tool"] == "bash" and "pytest" in c["input"].get("command", "")
                                    for c in failure_calls),
    }
```

### 8.4 典型缺陷模式与日志特征

| 缺陷模式 | 日志特征 | 对应低分维度 |
|---|---|---|
| **不运行测试就提交** | tool_calls 中无 `pytest` 命令 | 正确性 |
| **读取文件后不利用** | read_file 后 assistant 消息无相关引用 | 上下文管理 |
| **edit_file 盲目重试** | 连续多次 edit_file 同一文件且失败 | 工具使用 |
| **过早放弃** | 对话轮次 < 5 就停止 | 正确性、效率 |
| **冗余探索** | 连续读取 10+ 文件但只修改 1 个 | 效率、工具使用 |
| **忽略错误信息** | bash 返回 Error 后 assistant 未调整策略 | 正确性 |
| **压缩后丢失关键信息** | 压缩后重新询问已知信息 | 上下文管理 |

## 九、评估执行流程

### 9.1 完整评估流程

```
步骤 1：准备评估环境
├── 创建 eval_workspace/ 目录
├── 加载 Suite 定义（如 bug_fix_suite.json）
└── 为每个 Task 创建初始文件模板

步骤 2：执行评估
├── for each task in suite:
│   ├── for run_id in 1..5:
│   │   ├── 创建隔离工作目录 task_<id>_<run_id>/
│   │   ├── 复制初始文件到工作目录
│   │   ├── 启动 LoggedAgentLoop
│   │   ├── 运行 Agent（传入 prompt，超时 300s）
│   │   ├── 记录完整日志到 .agent_logs/
│   │   ├── 执行评估命令（pytest 等）
│   │   └── 运行评分器（6 个维度）
│   └── 聚合 5 次运行结果，计算通过率和统计指标
└── 聚合所有 Task 结果

步骤 3：生成评估报告
├── 各 Suite 的通过率统计
├── 各维度评分分布
├── 低分 Task 的日志归因分析
└── Agent 缺陷总结与改进建议
```

### 9.2 评估报告结构

```markdown
# Agent 评估报告

## 评估概览
- 评估时间：2026-08-05
- 评估范围：5 个 Suite，共 26 个 Task
- 每个 Task 运行 5 次
- 总运行次数：130 次

## 各 Suite 通过率统计
| Suite | Task 数 | 平均通过率 | A 级 | B 级 | C 级 | D 级 |
|---|---|---|---|---|---|---|
| bug_fix | 7 | 75% | 3 | 2 | 1 | 1 |
| feature_impl | 6 | 60% | 2 | 1 | 2 | 1 |
| refactor | 4 | 50% | 1 | 1 | 1 | 1 |
| code_review | 3 | 80% | 2 | 1 | 0 | 0 |
| debugging | 3 | 55% | 1 | 0 | 1 | 1 |

## 各维度评分分布
| 维度 | 平均分 | 最低分 | 最高分 |
|---|---|---|---|
| 正确性 | 72.5 | 20.0 | 100.0 |
| 无回归 | 85.0 | 50.0 | 100.0 |
| 代码质量 | 78.3 | 40.0 | 100.0 |
| 效率 | 65.2 | 30.0 | 95.0 |
| 工具使用 | 70.8 | 25.0 | 100.0 |
| 上下文管理 | 82.1 | 60.0 | 100.0 |

## 低分 Task 归因分析
### bugfix-race-003（通过率 20%）
- 缺陷模式：未运行测试就提交 + 忽略错误信息
- 日志证据：5 次运行中 4 次无 pytest 调用
- 改进建议：在 system prompt 中强调"修改后必须运行测试"

### feat-websocket-005（通过率 40%）
- 缺陷模式：冗余探索 + 工具调用过多
- 日志证据：平均工具调用 25 次（基线 10 次）
- 改进建议：引入规划阶段，减少盲目探索
```

## 十、评估配置与运行命令

### 10.1 评估配置文件

```json
{
  "eval_name": "agent_code_dev_eval_v1",
  "model": "claude-sonnet-4-6",
  "temperature": 0.7,
  "runs_per_task": 5,
  "timeout_per_run": 300,
  "max_tool_calls": 30,
  "suites": ["bug_fix", "feature_impl", "refactor", "code_review", "debugging"],
  "grader_weights": {
    "correctness": 0.30,
    "no_regression": 0.20,
    "code_quality": 0.15,
    "efficiency": 0.15,
    "tool_usage": 0.10,
    "context_mgmt": 0.10
  }
}
```

### 10.2 运行命令

```bash
# 运行完整评估
python eval/run_eval.py --config eval/config.json --output eval/results/

# 运行单个 Suite
python eval/run_eval.py --config eval/config.json --suite bug_fix --output eval/results/

# 运行单个 Task（调试用）
python eval/run_eval.py --config eval/config.json --task bugfix-parser-001 --runs 1 --output eval/results/

# 生成评估报告
python eval/generate_report.py --results eval/results/ --output eval/report.md
```

## 十一、总结

本评估方案的核心设计要点：

1. **对抗随机性**：每个 Task 运行 5 次，以通过率作为核心指标，避免单次结果的偶然性
2. **完整日志**：记录对话历史、工具调用、工具结果三类日志，支持事后归因分析
3. **执行验证**：通过实际运行 pytest、flake8 等工具验证代码结果，而非依赖 LLM 主观判断
4. **多维度评分**：6 个维度（正确性、无回归、代码质量、效率、工具使用、上下文管理）全面评估
5. **缺陷发现**：通过低分维度定位 + 日志归因分析，将评估结果转化为可执行的改进建议
